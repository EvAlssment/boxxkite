# helm-rendered.yaml is generated by ../generate.sh from deploy/helm/boxxkite.
# Keep the chart as the source of truth; do not add runtime resources here.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- helm-rendered.yaml
