# =============================================================================
# DIRECT DEPENDENCIES - humans edit this file.
# The sandbox image installs from `requirements.lock`, which pins every package
# direct and transitive for the Python 3.11 Linux runtime.
#
# After editing this file, regenerate the lockfile:
#   cd deploy && uv pip compile requirements.txt --python-version 3.11 --python-platform linux -o requirements.lock
# =============================================================================

# Data Processing
pandas==3.0.3
numpy==2.4.6
polars==1.42.1

# Visualization
matplotlib==3.11.0
seaborn==0.13.2
plotly==6.8.0

# Scientific Computing
# Capped below 1.18 -- scipy 1.18.0 requires Python>=3.12, incompatible with
# this image's Python 3.11 runtime (uv pip compile fails outright otherwise).
scipy==1.17.1
scikit-learn==1.9.0
statsmodels==0.14.6

# Excel/Spreadsheet Formats
openpyxl==3.1.5
xlrd==2.0.2
xlsxwriter==3.2.9

# Document Processing
python-docx==1.2.0
python-pptx==1.0.2
defusedxml==0.7.1
pypdf==6.14.2  # bumped to remediate SNYK-PYTHON-PYPDF-17378777 (High DoS) / CVE-2026-48155/48156/48735 (Medium DoS)
pdfplumber==0.11.10
reportlab==4.4.10
pypdfium2==5.11.0
pdf2image==1.17.0
pytesseract==0.3.13
markitdown[pptx]==0.1.6

# HTML/XML Parsing
lxml==6.1.1
beautifulsoup4==4.15.0
html5lib==1.1

# Data Serialization
pyyaml==6.0.3
toml==0.10.2
orjson==3.11.9

# Image Processing
pillow==12.3.0

# Date/Time
python-dateutil==2.9.0.post0
pytz==2026.2

# Output Formatting
tabulate==0.10.0
tqdm==4.68.3

# Templating
jinja2==3.1.6
markdownify==1.2.3

# Text Processing
chardet==7.4.3
regex==2026.6.28
ftfy==6.3.1
anyascii==0.3.3

# HTTP (for offline API mocking/testing)
requests==2.34.2
httpx==0.28.1
