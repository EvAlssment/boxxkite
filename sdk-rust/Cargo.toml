[package]
name = "boxxkite-client"
version = "0.2.4"
edition = "2021"
rust-version = "1.75"
description = "Rust client for a hosted boxxkite control-plane (sandboxes, exec, files, audit log, human takeover)"
license = "Apache-2.0"
repository = "https://github.com/EvAlssment/boxxkite"
readme = "README.md"
keywords = ["sandbox", "agent", "code-execution", "boxxkite"]
categories = ["api-bindings", "asynchronous"]

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "stream"] }
reqwest-eventsource = "0.6"
tokio-tungstenite = { version = "0.30", features = ["rustls-tls-webpki-roots"] }
tokio = { version = "1", features = ["net", "time"] }
url = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
async-stream = "0.3"
futures-util = "0.3"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "test-util"] }
wiremock = "0.6"
hmac = "0.12"
sha2 = "0.10"
hex = "0.4"
