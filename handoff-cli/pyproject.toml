[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "boxxkite-handoff"
version = "0.1.0"
description = "Hand off an in-progress Claude Code / Codex / opencode / Cursor session from your local machine into a fresh boxxkite sandbox, full conversation history included."
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
dependencies = [
    "boxxkite-client",
]

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.6"]

[project.scripts]
boxxkite-handoff = "boxxkite_handoff.cli:main"

[project.urls]
Homepage = "https://github.com/EvAlssment/boxxkite"
Repository = "https://github.com/EvAlssment/boxxkite"
Issues = "https://github.com/EvAlssment/boxxkite/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
